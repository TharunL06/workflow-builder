import React from 'react';

const ArrayMethodNode = ({ type }) => {
  const handleRun = () => {
   
  };

  return (
    <div>
      <h3>{type}</h3>
      <button onClick={handleRun}>Run</button>
    </div>
  );
};

export default ArrayMethodNode;
